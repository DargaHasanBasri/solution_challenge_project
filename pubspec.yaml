name: solution_challenge_project
description: A new Flutter project.

publish_to:

version: 1.0.0+1

environment:
  sdk: ">=2.19.2 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  get: ^4.6.5
  flutter_screenutil: ^5.7.0
  cupertino_icons: ^1.0.2
  flutter_svg:
  circular_seek_bar: ^1.1.0+2
  flutter_animation_progress_bar: ^2.3.1
  google_maps_flutter:

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/images/
    - assets/icons/
