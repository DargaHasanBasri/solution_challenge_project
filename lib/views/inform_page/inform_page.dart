import 'package:solution_challenge_project/export.dart';

class InformPage extends StatelessWidget {
  const InformPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text("inform page"),
      ),
    );
  }
}
